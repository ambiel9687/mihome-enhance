# Mihomo 自动更新容器 - 环境变量配置示例
# 使用方法：
# 1. 复制此文件为 .env
# 2. 根据实际情况修改配置
# 3. docker-compose 会自动读取 .env 文件

# ==================== 必需配置 ====================

# 订阅地址（必填）
# 支持：Clash 订阅链接、Workers 转换后的链接
SUBSCRIBE_URL=https://your-subscription-url-here

# ==================== Workers 转换服务 ====================

# 如果使用自己的 Workers 转换服务，填写这里
# 留空则直接使用 SUBSCRIBE_URL
WORKER_URL=

# ==================== 更新策略 ====================

# 更新间隔（秒）
# 建议值：3600（1小时）、7200（2小时）、14400（4小时）
UPDATE_INTERVAL=28800

# 首次更新延迟（秒）
# 容器启动后等待多久进行首次更新
# 建议值：300（5分钟）- 给 Mihomo 足够的初始化时间
INITIAL_UPDATE_DELAY=300

# ==================== 端口配置 ====================

# Socks5 起始端口
# 每个节点会分配一个独立端口，从这个端口开始递增
# 例如：START_PORT=42000，有 10 个节点，则使用 42000-42009
START_PORT=42000

# ==================== API 配置 ====================

# Mihomo API 密钥
# 用于访问 http://localhost:9090 的 RESTful API
# 建议使用强密码
API_SECRET=wangzh

# ==================== Socks5 认证 ====================

# Socks5 代理认证（可选）
# 如果需要为 Socks5 代理添加用户名密码认证，填写这里
AUTH_USER=
AUTH_PASS=

# ==================== 其他配置 ====================

# 自定义配置名称（可选）
# 用于生成配置文件名
CONFIG_NAME=

# 日志级别
# 可选值：debug, info, warning, error
LOG_LEVEL=info

# 时区设置
# 影响日志中的时间显示
TZ=Asia/Shanghai

# ==================== 高级配置 ====================

# Mihomo API 地址（通常不需要修改）
MIHOMO_API=http://localhost:9090

# 配置文件最大备份数量
# MAX_BACKUPS=3
